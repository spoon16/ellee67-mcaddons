// One shared logger for every Stair Sitting module, so all of its Content Log lines carry the same title.
import { featureLog } from "../../core/log.ts";

export const log = featureLog("Stair Sitting");
